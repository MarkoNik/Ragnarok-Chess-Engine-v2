<configuration>

    <!-- Console appender for stderr. Must stay on stderr, not the default stdout:
         stdout is the UCI protocol channel and must contain nothing else, or
         GUIs/automation tools trying to parse it get confused. -->
    <appender name="STDERR" class="ch.qos.logback.core.ConsoleAppender">
        <target>System.err</target>
        <encoder>
            <pattern>%d{yyyy-MM-dd HH:mm:ss} %level %logger{36} - %msg%n</pattern>
        </encoder>
    </appender>

    <!-- Rolling File appender -->
    <appender name="ROLLING_FILE" class="ch.qos.logback.core.rolling.RollingFileAppender">
        <rollingPolicy class="ch.qos.logback.core.rolling.TimeBasedRollingPolicy">
            <!-- Set the directory where logs will be stored -->
            <fileNamePattern>./log/ragnarok.%d{yyyy-MM-dd}.log</fileNamePattern>
            <!-- Keep 30 days worth of logs -->
            <maxHistory>30</maxHistory>
        </rollingPolicy>
        <encoder>
            <pattern>%d{yyyy-MM-dd HH:mm:ss} %level %logger{36} - %msg%n</pattern>
        </encoder>
    </appender>

    <root level="debug">
        <!-- Reference STDERR appender -->
        <appender-ref ref="STDERR" />
        <!-- Reference Rolling File appender -->
        <appender-ref ref="ROLLING_FILE" />
    </root>

</configuration>
