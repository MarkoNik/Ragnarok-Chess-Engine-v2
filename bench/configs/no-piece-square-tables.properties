# Isolates piece-square tables' contribution: with this off, the eval falls
# back to material-only (Evaluator.evaluatePieces instead of evaluateAdvanced),
# ignoring piece placement entirely.
PieceSquareTables=false
