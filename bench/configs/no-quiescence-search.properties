# Isolates quiescence search's contribution: with this off, the search
# evaluates leaf nodes statically instead of resolving captures first,
# which normally avoids the horizon effect (stopping mid-exchange).
QuiescenceSearch=false
