# All features enabled - the engine's default behavior. Listed explicitly
# here so it's a documented, reusable profile like the ablation ones below,
# not just "whatever the code defaults to".
NullMovePruning=true
TranspositionTable=true
QuiescenceSearch=true
PieceSquareTables=true
